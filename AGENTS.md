## IMPORTANTE

- Este app e uma copia de `C:\xampp\htdocs\apec-rodrigo`, adaptada para o projeto `apec-roberto`.
- Endpoint padrao de deploy: `https://apec-roberto.azurewebsites.net`
- Em ambiente local, os endpoints PHP podem ler as credenciais do arquivo `.env` na raiz do projeto.
